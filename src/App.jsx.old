import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import Login from './pages/Login';
import Layout from './components/Layout';

// Admin Pages
import AdminDashboard from './pages/Admin/Dashboard';
import ProdiManagement from './pages/Admin/ProdiManagement';
import DosenManagement from './pages/Admin/DosenManagement';

// Prodi Pages (placeholder for now)
import Dashboard from './pages/Dashboard';

// Dosen Pages
import DosenDashboard from './pages/Dosen/Dashboard';
import RPSList from './pages/Dosen/RPSList';

// --- MOCK DATA (Data Palsu untuk Demo ke Tim Backend) ---
const initialCourses = [
  {
    id: 1,
    code: 'TI-101',
    name: 'Dasar Pemrograman',
    semester: 1,
    sks: 3,
    status: 'completed', // completed, draft, not_created
    lastUpdated: '2024-03-10',
    description: 'Mata kuliah ini membahas dasar-dasar logika pemrograman menggunakan Python.',
    meetings: [
      { week: 1, topic: 'Pengenalan Algoritma', method: 'Ceramah' },
      { week: 2, topic: 'Variabel dan Tipe Data', method: 'Lab' }
    ]
  },
  {
    id: 2,
    code: 'TI-205',
    name: 'Struktur Data',
    semester: 3,
    sks: 4,
    status: 'draft',
    lastUpdated: '2024-03-12',
    description: 'Mempelajari pengorganisasian data di memori komputer.',
    meetings: []
  },
  {
    id: 3,
    code: 'TI-309',
    name: 'Kecerdasan Buatan',
    semester: 5,
    sks: 3,
    status: 'not_created',
    lastUpdated: '-',
    description: '',
    meetings: []
  }
];

export default function App() {
  // State Management
  const [userRole, setUserRole] = useState('kaprodi'); // 'kaprodi' or 'dosen'
  const [view, setView] = useState('dashboard'); // 'dashboard', 'editor', 'generator'
  const [courses, setCourses] = useState(initialCourses);
  const [selectedCourse, setSelectedCourse] = useState(null);
  
  // State khusus AI Generator
  const [aiPrompt, setAiPrompt] = useState('');
  const [isGenerating, setIsGenerating] = useState(false);
  const [generatedResult, setGeneratedResult] = useState(null);

  // --- FUNGSI NAVIGASI & LOGIC ---

  const handleEdit = (course) => {
    setSelectedCourse(course);
    setView('editor');
  };

  const handleGenerateClick = () => {
    setAiPrompt('');
    setGeneratedResult(null);
    setView('generator');
  };

  // Simulasi Fungsi AI (Untuk teman tim AI & Backend paham)
  const handleSimulateAI = () => {
    if (!aiPrompt) return;
    setIsGenerating(true);

    // Simulasi delay request ke Backend/AI Model
    setTimeout(() => {
      setIsGenerating(false);
      setGeneratedResult({
        name: aiPrompt,
        description: `Mata kuliah ${aiPrompt} dirancang untuk memberikan pemahaman mendalam mengenai konsep fundamental dan aplikasi praktis di industri terkini.`,
        meetings: [
          { week: 1, topic: `Pengantar ${aiPrompt}`, method: 'Ceramah & Diskusi' },
          { week: 2, topic: 'Konsep Dasar & Sejarah', method: 'Studi Kasus' },
          { week: 3, topic: 'Implementasi Tahap Awal', method: 'Project Based Learning' },
          { week: 4, topic: 'Analisis dan Evaluasi', method: 'Presentasi Kelompok' },
          { week: 8, topic: 'Ujian Tengah Semester', method: 'Ujian Tulis' },
          { week: 16, topic: 'Ujian Akhir Semester', method: 'Project Final' },
        ]
      });
    }, 2500); // Delay 2.5 detik seolah-olah mikir
  };

  const handleSaveGeneratedRPS = () => {
    const newCourse = {
      id: courses.length + 1,
      code: `TI-${Math.floor(Math.random() * 900) + 100}`,
      name: generatedResult.name,
      semester: 1, // Default
      sks: 3, // Default
      status: 'draft',
      lastUpdated: 'Baru saja',
      description: generatedResult.description,
      meetings: generatedResult.meetings
    };

    setCourses([newCourse, ...courses]);
    setView('dashboard');
    alert("RPS Berhasil digenerate oleh AI dan disimpan ke Draft!");
  };

  // --- COMPONENTS UI ---

  // 1. Sidebar Component
  const Sidebar = () => (
    <div className="w-64 bg-slate-900 text-white h-screen fixed left-0 top-0 flex flex-col shadow-xl">
      <div className="p-6 border-b border-slate-800 flex items-center gap-2">
        <div className="bg-blue-600 p-2 rounded-lg">
          <BookOpen size={24} />
        </div>
        <h1 className="text-xl font-bold tracking-tight">SmartRPS</h1>
      </div>
      
      <div className="p-4 flex-1">
        <p className="text-xs text-slate-500 uppercase font-semibold mb-2">Menu Utama</p>
        <button 
          onClick={() => setView('dashboard')}
          className={`w-full flex items-center gap-3 px-4 py-3 rounded-lg mb-1 transition-all ${view === 'dashboard' ? 'bg-blue-600 text-white shadow-lg shadow-blue-900/50' : 'text-slate-400 hover:bg-slate-800 hover:text-white'}`}
        >
          <FileText size={18} />
          Daftar RPS
        </button>
        
        {/* Fitur khusus Kaprodi */}
        {userRole === 'kaprodi' && (
          <button 
            onClick={handleGenerateClick}
            className={`w-full flex items-center gap-3 px-4 py-3 rounded-lg mb-1 transition-all ${view === 'generator' ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-900/50' : 'text-indigo-300 hover:bg-indigo-900/30 hover:text-indigo-200'}`}
          >
            <Bot size={18} />
            Generate AI
            <span className="bg-indigo-500 text-[10px] px-1.5 py-0.5 rounded text-white ml-auto">PRO</span>
          </button>
        )}
      </div>

      <div className="p-4 border-t border-slate-800">
        <div className="bg-slate-800 rounded-xl p-4">
          <div className="flex items-center gap-3 mb-3">
            <div className="bg-slate-700 p-2 rounded-full">
              <User size={16} />
            </div>
            <div>
              <p className="text-sm font-medium">{userRole === 'kaprodi' ? 'Budi (Kaprodi)' : 'Siti (Dosen)'}</p>
              <p className="text-xs text-slate-400">{userRole === 'kaprodi' ? 'Admin Access' : 'Lecturer Access'}</p>
            </div>
          </div>
          
          <div className="text-xs text-slate-500 mb-2 font-medium uppercase">Switch Role (Demo):</div>
          <div className="flex bg-slate-900 p-1 rounded-lg">
            <button 
              onClick={() => setUserRole('kaprodi')}
              className={`flex-1 text-xs py-1.5 rounded-md transition-all ${userRole === 'kaprodi' ? 'bg-blue-600 text-white' : 'text-slate-400'}`}
            >
              Kaprodi
            </button>
            <button 
              onClick={() => setUserRole('dosen')}
              className={`flex-1 text-xs py-1.5 rounded-md transition-all ${userRole === 'dosen' ? 'bg-blue-600 text-white' : 'text-slate-400'}`}
            >
              Dosen
            </button>
          </div>
        </div>
      </div>
    </div>
  );

  // 2. Dashboard View
  const DashboardView = () => (
    <div className="p-8">
      <header className="flex justify-between items-center mb-8">
        <div>
          <h2 className="text-2xl font-bold text-slate-800">Manajemen RPS</h2>
          <p className="text-slate-500">Daftar Rencana Pembelajaran Semester Program Studi</p>
        </div>
        {userRole === 'kaprodi' && (
          <button 
            onClick={handleGenerateClick}
            className="bg-indigo-600 hover:bg-indigo-700 text-white px-5 py-2.5 rounded-lg flex items-center gap-2 shadow-lg shadow-indigo-200 transition-all"
          >
            <Bot size={18} />
            Buat RPS Baru dengan AI
          </button>
        )}
      </header>

      {/* Stats Cards */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <div className="bg-white p-6 rounded-xl border border-slate-100 shadow-sm flex items-center gap-4">
          <div className="p-3 bg-green-100 text-green-600 rounded-lg"><CheckCircle size={24}/></div>
          <div><p className="text-2xl font-bold text-slate-800">{courses.filter(c => c.status === 'completed').length}</p><p className="text-sm text-slate-500">RPS Selesai</p></div>
        </div>
        <div className="bg-white p-6 rounded-xl border border-slate-100 shadow-sm flex items-center gap-4">
          <div className="p-3 bg-yellow-100 text-yellow-600 rounded-lg"><Edit3 size={24}/></div>
          <div><p className="text-2xl font-bold text-slate-800">{courses.filter(c => c.status === 'draft').length}</p><p className="text-sm text-slate-500">Dalam Draft</p></div>
        </div>
        <div className="bg-white p-6 rounded-xl border border-slate-100 shadow-sm flex items-center gap-4">
          <div className="p-3 bg-red-100 text-red-600 rounded-lg"><FileText size={24}/></div>
          <div><p className="text-2xl font-bold text-slate-800">{courses.filter(c => c.status === 'not_created').length}</p><p className="text-sm text-slate-500">Belum Ada RPS</p></div>
        </div>
      </div>

      {/* Course Table */}
      <div className="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden">
        <table className="w-full text-left text-sm">
          <thead className="bg-slate-50 text-slate-600 font-semibold border-b border-slate-200">
            <tr>
              <th className="px-6 py-4">Kode</th>
              <th className="px-6 py-4">Mata Kuliah</th>
              <th className="px-6 py-4">Semester</th>
              <th className="px-6 py-4">Status</th>
              <th className="px-6 py-4">Terakhir Update</th>
              <th className="px-6 py-4 text-right">Aksi</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-slate-100">
            {courses.map(course => (
              <tr key={course.id} className="hover:bg-slate-50 transition-colors">
                <td className="px-6 py-4 font-mono text-slate-500">{course.code}</td>
                <td className="px-6 py-4 font-medium text-slate-900">{course.name}</td>
                <td className="px-6 py-4 text-slate-600">Sem {course.semester} ({course.sks} SKS)</td>
                <td className="px-6 py-4">
                  <span className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                    ${course.status === 'completed' ? 'bg-green-100 text-green-800' : 
                      course.status === 'draft' ? 'bg-yellow-100 text-yellow-800' : 'bg-red-100 text-red-800'}`}>
                    {course.status === 'completed' ? 'Selesai' : course.status === 'draft' ? 'Draft' : 'Belum Ada'}
                  </span>
                </td>
                <td className="px-6 py-4 text-slate-500">{course.lastUpdated}</td>
                <td className="px-6 py-4 text-right">
                  <button 
                    onClick={() => handleEdit(course)}
                    className="text-blue-600 hover:text-blue-800 font-medium px-3 py-1 rounded hover:bg-blue-50 transition-colors"
                  >
                    {userRole === 'kaprodi' ? 'Review' : 'Edit'}
                  </button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );

  // 3. AI Generator View (Fitur Utama Kaprodi)
  const AIGeneratorView = () => (
    <div className="p-8 max-w-4xl mx-auto h-screen flex flex-col justify-center">
      <button onClick={() => setView('dashboard')} className="text-slate-500 hover:text-slate-800 mb-6 flex items-center gap-2">
        ← Kembali ke Dashboard
      </button>

      <div className="bg-white rounded-2xl shadow-xl border border-indigo-100 overflow-hidden">
        {/* Header Generator */}
        <div className="bg-indigo-600 p-8 text-center text-white relative overflow-hidden">
          <div className="relative z-10">
            <div className="bg-indigo-500 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4 shadow-lg">
              <Bot size={32} />
            </div>
            <h2 className="text-3xl font-bold mb-2">AI RPS Generator</h2>
            <p className="text-indigo-100">Masukkan nama mata kuliah, AI akan menyusun strukturnya.</p>
          </div>
          {/* Background decoration */}
          <div className="absolute top-0 left-0 w-full h-full opacity-10 pointer-events-none">
            <div className="absolute top-10 left-10 w-20 h-20 bg-white rounded-full blur-2xl"></div>
            <div className="absolute bottom-10 right-10 w-32 h-32 bg-white rounded-full blur-3xl"></div>
          </div>
        </div>

        <div className="p-8">
          {!generatedResult ? (
            <div className="max-w-xl mx-auto">
              <label className="block text-sm font-medium text-slate-700 mb-2">Nama Mata Kuliah Baru</label>
              <div className="flex gap-3 mb-6">
                <input 
                  type="text" 
                  value={aiPrompt}
                  onChange={(e) => setAiPrompt(e.target.value)}
                  placeholder="Contoh: Digital Marketing, Pemrograman Mobile..."
                  className="flex-1 border border-slate-300 rounded-lg px-4 py-3 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-all"
                />
                <button 
                  onClick={handleSimulateAI}
                  disabled={isGenerating || !aiPrompt}
                  className="bg-indigo-600 text-white px-6 py-3 rounded-lg font-medium hover:bg-indigo-700 disabled:bg-indigo-300 disabled:cursor-not-allowed flex items-center gap-2 transition-all"
                >
                  {isGenerating ? <Loader2 className="animate-spin" /> : <Bot size={20} />}
                  {isGenerating ? 'Sedang Berpikir...' : 'Generate'}
                </button>
              </div>

              <div className="bg-slate-50 p-4 rounded-lg border border-slate-200 text-sm text-slate-600">
                <p className="font-semibold mb-1 flex items-center gap-2"><Users size={16}/> Info untuk Tim:</p>
                <p>Di tahap ini, frontend akan mengirim string nama matkul ke Backend. Backend akan memanggil API AI Model kalian, lalu mengembalikan JSON struktur RPS.</p>
              </div>
            </div>
          ) : (
            // Result Preview
            <div className="animate-fade-in">
              <div className="flex items-center justify-between mb-6">
                <h3 className="text-xl font-bold text-slate-800">Draft RPS: {generatedResult.name}</h3>
                <span className="bg-green-100 text-green-700 px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wide">Generated by AI</span>
              </div>
              
              <div className="bg-slate-50 p-6 rounded-xl border border-slate-200 mb-6 max-h-64 overflow-y-auto">
                <div className="mb-4">
                  <h4 className="text-xs font-bold text-slate-500 uppercase mb-1">Deskripsi Matakuliah</h4>
                  <p className="text-slate-800">{generatedResult.description}</p>
                </div>
                <div>
                  <h4 className="text-xs font-bold text-slate-500 uppercase mb-2">Rencana Pertemuan (Sampel)</h4>
                  <ul className="space-y-2">
                    {generatedResult.meetings.map((m, idx) => (
                      <li key={idx} className="bg-white p-3 rounded border border-slate-200 flex justify-between items-center">
                        <span className="font-medium text-slate-700">Minggu {m.week}: {m.topic}</span>
                        <span className="text-xs bg-slate-100 px-2 py-1 rounded text-slate-500">{m.method}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>

              <div className="flex gap-3 justify-end">
                <button 
                  onClick={() => setGeneratedResult(null)}
                  className="px-5 py-2.5 text-slate-600 font-medium hover:bg-slate-100 rounded-lg"
                >
                  Buang & Ulangi
                </button>
                <button 
                  onClick={handleSaveGeneratedRPS}
                  className="px-5 py-2.5 bg-indigo-600 text-white font-medium hover:bg-indigo-700 rounded-lg flex items-center gap-2 shadow-lg shadow-indigo-200"
                >
                  <Save size={18} />
                  Simpan ke Database
                </button>
              </div>
            </div>
          )}
        </div>
      </div>
    </div>
  );

  // 4. Editor View (Tempat Dosen/Kaprodi Edit Manual)
  const EditorView = () => (
    <div className="p-8 pb-32">
      <button onClick={() => setView('dashboard')} className="text-slate-500 hover:text-slate-800 mb-6 flex items-center gap-2">
        ← Kembali ke Dashboard
      </button>

      <div className="bg-white rounded-xl shadow-sm border border-slate-200 p-8 max-w-5xl mx-auto">
        <header className="border-b border-slate-100 pb-6 mb-6 flex justify-between items-start">
          <div>
            <h2 className="text-2xl font-bold text-slate-900">{selectedCourse.name}</h2>
            <p className="text-slate-500 font-mono text-sm mt-1">{selectedCourse.code} • Semester {selectedCourse.semester} • {selectedCourse.sks} SKS</p>
          </div>
          <div className="flex gap-2">
             <button className="flex items-center gap-2 px-4 py-2 border border-slate-300 rounded-lg text-slate-600 hover:bg-slate-50">
               <FileText size={16} /> Export PDF
             </button>
             <button onClick={() => setView('dashboard')} className="flex items-center gap-2 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 shadow-sm">
               <Save size={16} /> Simpan Perubahan
             </button>
          </div>
        </header>

        {/* Form Editor */}
        <div className="space-y-8">
          <section>
            <h3 className="text-lg font-semibold text-slate-800 mb-3 border-l-4 border-blue-500 pl-3">Deskripsi Mata Kuliah</h3>
            <textarea 
              className="w-full border border-slate-300 rounded-lg p-4 min-h-[100px] focus:ring-2 focus:ring-blue-500 outline-none"
              defaultValue={selectedCourse.description || "Belum ada deskripsi."}
            />
          </section>

          <section>
            <h3 className="text-lg font-semibold text-slate-800 mb-3 border-l-4 border-blue-500 pl-3">Rencana Pembelajaran Mingguan</h3>
            <div className="border border-slate-200 rounded-lg overflow-hidden">
              <table className="w-full text-left text-sm">
                <thead className="bg-slate-50 text-slate-700 font-semibold">
                  <tr>
                    <th className="px-4 py-3 w-20">Minggu</th>
                    <th className="px-4 py-3">Topik / Bahan Kajian</th>
                    <th className="px-4 py-3 w-48">Metode</th>
                    <th className="px-4 py-3 w-24">Aksi</th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-slate-100">
                  {selectedCourse.meetings && selectedCourse.meetings.length > 0 ? (
                    selectedCourse.meetings.map((m, idx) => (
                      <tr key={idx} className="bg-white">
                        <td className="px-4 py-3 text-center font-medium">{m.week}</td>
                        <td className="px-4 py-3"><input type="text" defaultValue={m.topic} className="w-full border-b border-transparent hover:border-slate-300 focus:border-blue-500 outline-none py-1"/></td>
                        <td className="px-4 py-3"><input type="text" defaultValue={m.method} className="w-full border-b border-transparent hover:border-slate-300 focus:border-blue-500 outline-none py-1"/></td>
                        <td className="px-4 py-3 text-red-500 cursor-pointer hover:underline">Hapus</td>
                      </tr>
                    ))
                  ) : (
                    <tr>
                      <td colSpan="4" className="p-8 text-center text-slate-400 italic">
                        Belum ada data pertemuan. {userRole === 'kaprodi' ? 'Gunakan fitur AI Generator untuk mengisi otomatis!' : 'Silakan input manual.'}
                      </td>
                    </tr>
                  )}
                </tbody>
              </table>
              <div className="bg-slate-50 p-2 text-center border-t border-slate-200">
                <button className="text-blue-600 text-sm font-medium hover:underline flex items-center justify-center gap-1 w-full">
                  <Plus size={14} /> Tambah Pertemuan
                </button>
              </div>
            </div>
          </section>
        </div>
      </div>
    </div>
  );

  return (
    <div className="flex bg-slate-50 min-h-screen font-sans text-slate-900">
      <Sidebar />
      <main className="ml-64 flex-1">
        <div className="bg-white border-b border-slate-200 px-8 py-3 flex justify-between items-center sticky top-0 z-10">
          <div className="flex items-center bg-slate-100 rounded-lg px-3 py-1.5 w-96">
            <Search size={18} className="text-slate-400 mr-2" />
            <input type="text" placeholder="Cari mata kuliah..." className="bg-transparent border-none outline-none text-sm w-full" />
          </div>
          <div className="flex gap-4">
             {/* Notification/Profile mockups could go here */}
          </div>
        </div>

        {view === 'dashboard' && <DashboardView />}
        {view === 'generator' && <AIGeneratorView />}
        {view === 'editor' && <EditorView />}
      </main>
    </div>
  );
}